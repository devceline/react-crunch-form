{
  "name": "react-crunch-form",
  "version": "0.0.1",
  "description": "A library that enables you to quickly crunch out a form that performs as you would expect",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "git@github.com:shaheensarafa/react-crunch-form.git",
  "author": "Shaheen Sarafa",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "yarn build",
    "prepublishOnly": "yarn lint",
    "preversion": "yarn lint",
    "version": "yarn format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "license": "MIT",
  "private": false,
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "@types/react": "^17.0.3",
    "prettier": "^2.2.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "react": "^17.0.1"
  }
}
